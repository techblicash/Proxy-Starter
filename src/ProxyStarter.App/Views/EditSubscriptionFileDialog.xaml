<ui:FluentWindow x:Class="ProxyStarter.App.Views.EditSubscriptionFileDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="{DynamicResource Text_EditSubscriptionFile}"
                 Width="920"
                 Height="700"
                 MinWidth="720"
                 MinHeight="520"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 ExtendsContentIntoTitleBar="True"
                 Background="Transparent"
                 UseLayoutRounding="True"
                 SnapsToDevicePixels="True"
                 TextOptions.TextFormattingMode="Display"
                 TextOptions.TextRenderingMode="ClearType"
                 TextOptions.TextHintingMode="Fixed"
                 FontFamily="Segoe UI Variable">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText"
                   Text="{DynamicResource Text_EditSubscriptionFile}"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6" />

        <TextBlock Grid.Row="1"
                   Text="{DynamicResource Text_SubscriptionFileHint}"
                   Opacity="0.7"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <Border Grid.Row="2"
                Background="{DynamicResource LayerOnAcrylicFillColorDefaultBrush}"
                BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="6">
            <TextBox x:Name="EditorBox"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     Background="Transparent"
                     FontFamily="Cascadia Mono,Consolas"
                     FontSize="12" />
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <ui:Button Content="{DynamicResource Text_Cancel}" Click="OnCancelClick" Margin="0,0,8,0" />
            <ui:Button x:Name="SaveButton" Content="{DynamicResource Text_Save}" Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>

