<ui:FluentWindow x:Class="ProxyStarter.App.Views.AddSubscriptionDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="{DynamicResource Text_AddSubscription}"
                 Width="520"
                 MinHeight="380"
                 MaxHeight="600"
                 SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 ExtendsContentIntoTitleBar="True"
                 Background="Transparent"
                 ResizeMode="NoResize"
                 UseLayoutRounding="True"
                 SnapsToDevicePixels="True"
                 FontFamily="Segoe UI Variable">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText"
                   Text="{DynamicResource Text_CreateSubscription}"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="{DynamicResource Text_Name}" Opacity="0.7" />
            <TextBox x:Name="NameBox"
                     MinHeight="40"
                     Padding="12,0"
                     VerticalContentAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{DynamicResource Text_URL}" Opacity="0.7" />
            <TextBox x:Name="UrlBox"
                     MinHeight="40"
                     Padding="12,0"
                     VerticalContentAlignment="Center" />
            <TextBlock Text="{DynamicResource Text_SupportsFormats}"
                       Opacity="0.6"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{DynamicResource Text_AutoUpdate}" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                <CheckBox x:Name="AutoUpdateBox"
                          Content="{DynamicResource Text_Enable}"
                          IsChecked="True"
                          VerticalAlignment="Center" />
                <TextBlock Text="{DynamicResource Text_IntervalMinutes}"
                           Margin="16,0,8,0"
                           VerticalAlignment="Center"
                           Opacity="0.7" />
                <TextBox x:Name="AutoUpdateIntervalBox"
                         Width="120"
                         MinHeight="40"
                         Padding="12,0"
                         VerticalContentAlignment="Center"
                         Text="360" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button Content="{DynamicResource Text_Cancel}" Click="OnCancelClick" Margin="0,0,8,0" />
            <ui:Button x:Name="ConfirmButton" Content="{DynamicResource Text_Add}" Click="OnAddClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
