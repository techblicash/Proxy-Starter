<ui:FluentWindow x:Class="ProxyStarter.App.Views.TrayMenuWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:models="clr-namespace:ProxyStarter.App.Models"
                 mc:Ignorable="d"
                 Width="420"
                 Height="560"
                 MinWidth="360"
                 MinHeight="460"
                 WindowStyle="None"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False"
                 WindowBackdropType="Acrylic"
                 WindowCornerPreference="Round"
                 ExtendsContentIntoTitleBar="True"
                 Background="Transparent"
                 UseLayoutRounding="True"
                 SnapsToDevicePixels="True"
                 FontFamily="Segoe UI Variable">
    <ui:FluentWindow.Resources>
        <Style x:Key="TrayActionButtonStyle"
               TargetType="ui:Button"
               BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Height" Value="46" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Appearance" Value="Secondary" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="MouseOverBackground" Value="{DynamicResource AccentFillColorSecondaryBrush}" />
            <Setter Property="MouseOverBorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
            <Setter Property="PressedBackground" Value="{DynamicResource AccentFillColorDefaultBrush}" />
            <Setter Property="PressedBorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
            <Setter Property="PressedForeground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </ui:FluentWindow.Resources>

    <Border Margin="8"
            Padding="14"
            CornerRadius="16"
            Background="{DynamicResource LayerOnAcrylicFillColorDefaultBrush}"
            BorderBrush="{DynamicResource SurfaceStrokeColorFlyoutBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="4,2,4,0">
                <TextBlock Text="{DynamicResource Text_AppName}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="Quick Control"
                           Margin="0,4,0,0"
                           FontSize="12"
                           Opacity="0.72" />
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ui:Button Grid.Row="0" Grid.Column="0"
                           Margin="0,0,8,8"
                           HorizontalAlignment="Stretch"
                           CornerRadius="10"
                           Style="{StaticResource TrayActionButtonStyle}"
                           Content="{DynamicResource Text_Open}"
                           Icon="{ui:SymbolIcon Home24}"
                           Click="OnOpenClicked" />

                <ui:Button Grid.Row="0" Grid.Column="1"
                           Margin="0,0,0,8"
                           HorizontalAlignment="Stretch"
                           CornerRadius="10"
                           Style="{StaticResource TrayActionButtonStyle}"
                           Content="{Binding CoreToggleLabel}"
                           Icon="{ui:SymbolIcon Power24}"
                           Click="OnToggleCoreClicked" />

                <ui:Button Grid.Row="1" Grid.Column="0"
                           Margin="0,0,8,0"
                           HorizontalAlignment="Stretch"
                           CornerRadius="10"
                           Style="{StaticResource TrayActionButtonStyle}"
                           Content="{DynamicResource Text_Refresh}"
                           Icon="{ui:SymbolIcon ArrowSync24}"
                           Click="OnRefreshNodesClicked" />

                <ui:Button Grid.Row="1" Grid.Column="1"
                           Margin="0,0,0,0"
                           HorizontalAlignment="Stretch"
                           CornerRadius="10"
                           Style="{StaticResource TrayActionButtonStyle}"
                           Content="{DynamicResource Text_Exit}"
                           Icon="{ui:SymbolIcon Dismiss24}"
                           Click="OnExitClicked" />
            </Grid>

            <Border Grid.Row="2"
                    Height="1"
                    Margin="0,14,0,12"
                    Background="{DynamicResource DividerStrokeColorDefaultBrush}"
                    Opacity="0.8" />

            <Grid Grid.Row="3" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{DynamicResource Text_Node}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />

                <TextBlock x:Name="NodesGroupText"
                           Grid.Column="1"
                           Opacity="0.7"
                           VerticalAlignment="Center" />
            </Grid>

            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ui:TextBox x:Name="NodeSearchBox"
                            Height="38"
                            Margin="0,0,0,10"
                            FontSize="14"
                            VerticalContentAlignment="Center"
                            PlaceholderText="{DynamicResource Text_SearchPlaceholder}"
                            TextChanged="OnNodeSearchTextChanged" />

                <Border Grid.Row="1"
                        Background="{DynamicResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="4">
                    <ui:ListView x:Name="NodesList"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontSize="14"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 VirtualizingStackPanel.ScrollUnit="Pixel"
                                 SelectionChanged="OnNodeSelectionChanged">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:ProxyNode}">
                                <Grid Margin="8,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />

                                    <ui:SymbolIcon x:Name="ActiveIcon"
                                                   Grid.Column="1"
                                                   Symbol="Checkmark24"
                                                   Margin="8,0,0,0"
                                                   Foreground="{DynamicResource AccentFillColorDefaultBrush}"
                                                   Visibility="Collapsed" />
                                </Grid>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                        <Setter TargetName="ActiveIcon" Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                    </ui:ListView>
                </Border>
            </Grid>

            <TextBlock x:Name="NodesStatusText"
                       Grid.Row="5"
                       Margin="4,10,4,0"
                       FontSize="13"
                       Opacity="0.72"
                       TextWrapping="Wrap" />
        </Grid>
    </Border>
</ui:FluentWindow>
